#! /usr/bin/env bash

TMP="tmp.txt"
FILE="file.txt"

echo "" > "$TMP"
for (( i=0; i < 10; i++ )); do
  echo "$i " >> "$TMP"
done

TO_SORT=$(cat "$TMP" | sort -R | paste -sd ' ')
rm "$TMP"

./push_swap $TO_SORT
